//-------------------------------------------------
//エフェクト汎用スーパークラス
//-------------------------------------------------
#include "BEffect.h"
#include "MyPG.h"

//-------------------------------------------------
//アニメーション
void BEffect::Anim()
{
	for (int i = 0; i < 5; ++i) {
		for (int j = 0; j < 6; ++j) {
			this->effImg[i*j + j] = ML::Box2D(j * 192, i * 192, 192, 192);
		}
	}
}